from tkinter import *
from tkinter.ttk import *

root = Tk()

# root.title("Hello")
# label = Label(root, text="Chào bạn").pack()

# frame = Frame(root)
# frame.pack()
# button = Button(frame, text="Button")
# button.pack()

# root.geometry("100x100")
# btn = Button(root, text = 'Click me !', command = root.destroy)
# btn.pack(side = 'top')

# root.geometry("450x300")
# user_name = Label(root, text="Username: ").place(x=40, y=60)
# user_pass = Label(root, text="Password: ").place(x=40, y=100)
# submit_btn = Button(root, text="Submit").place(x=40, y=140)
# name_input = Entry(root).place(x=120, y=60)
# pass_input = Entry(root).place(x=120, y=100)

# root.geometry("175x175")
# v = StringVar(root, "1")
# style = Style(root)
# style.configure("TRadiobutton", background="light blue",
#                 foreground="purple", font=("arial", 10, "bold"))
# values = {"RadioButton 1": "1",
#           "RadioButton 2": "2",
#           "RadioButton 3": "3",
#           "RadioButton 4": "4",
#           "RadioButton 5": "5"}
# for (text, value) in values.items():
#     Radiobutton(root, text=text, variable=v, value=value).pack(fill=X, ipady=5)


root.mainloop()
